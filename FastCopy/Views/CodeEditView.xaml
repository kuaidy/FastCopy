<Window x:Class="FastCopy.Views.CodeEditView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FastCopy.Views"
        mc:Ignorable="d"
        Title="添加脚本" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal">
            <Button Width="30" Height="30" ToolTip="保存" Command="{Binding SaveCodeCommand}" Background="{x:Null}" Margin="5">
                <StackPanel>
                    <Image Width="25" Height="25" Source="/Images/save.png" />
                </StackPanel>
            </Button>
            <Button Width="30" Height="30" ToolTip="执行" Command="{Binding RunCodeCommand}" Background="{x:Null}" Margin="5">
                <StackPanel>
                    <Image Width="25" Height="25" Source="/Images/run.png" />
                </StackPanel>
            </Button>
            <ComboBox SelectedIndex="0" VerticalContentAlignment="Center" Height="30" Margin="5" SelectedValuePath="Content" SelectedValue="{Binding CodeType}">
                <ComboBoxItem>C#</ComboBoxItem>
                <ComboBoxItem>Python</ComboBoxItem>
                <ComboBoxItem>JavaScript</ComboBoxItem>
            </ComboBox>
        </StackPanel>
        <avalonEdit:TextEditor
            Grid.Row="1"
            xmlns:avalonEdit="http://icsharpcode.net/sharpdevelop/avalonedit"
            Name="CodeEditor"
            ShowLineNumbers="True"
            SyntaxHighlighting="C#"
            FontFamily="Consolas"
            FontSize="10pt"
            VerticalScrollBarVisibility="Auto"
            HorizontalScrollBarVisibility="Auto"
            />
    </Grid>
</Window>
